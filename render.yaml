services:
  - type: web
    name: finswitch-api
    runtime: docker
    repo: https://github.com/OK45batwal/FINSWITCH
    dockerfilePath: backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
